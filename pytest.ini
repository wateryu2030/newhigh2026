[pytest]
pythonpath = gateway/src execution-engine/src core/src
addopts = --import-mode=importlib
testpaths = tests gateway/tests
markers =
    integration: 依赖外部服务或可选驱动（CI 默认跳过）
filterwarnings =
    ignore::DeprecationWarning

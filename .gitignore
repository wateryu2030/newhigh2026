# Virtual environment
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Node (frontend)
node_modules/
frontend/dist/
frontend/.next/

# macOS
.DS_Store

# Env files (secrets)
.env
.env.local
*.pem

# Local data (A-share DuckDB 由 scripts/copy_astock_duckdb_to_newhigh.py 复制而来)
data/
# DuckDB 备份目录（单文件可达数百 MB，GitHub 拒绝 ≥100MB）
backups/
# 根目录旧备份 DuckDB（真正的数据库在 data/ 下）
quant_system.duckdb

# OpenClaw 单轮执行状态（可选保留以追踪上次运行）
scripts/.openclaw_state.json

# 定时任务 / 全量周期日志
logs/

# 本地 pylint 报告（不入库）
evolution/pylint_report*.txt
evolution/daily_summary_*.md
# OpenClaw→Cursor 每轮自动生成的规划快照（本地留存即可）
evolution/openclaw_cursor_last_plan.md

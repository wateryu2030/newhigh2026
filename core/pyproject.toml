[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "core"
version = "0.1.0"
description = "Shared types and utilities for newhigh"
requires-python = ">=3.10"
dependencies = ["duckdb>=0.9.0"]

[tool.setuptools.packages.find]
where = ["src"]

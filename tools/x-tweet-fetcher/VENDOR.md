# 并入 newhigh 说明

本目录原为 Git 子模块（上游：<https://github.com/ythx-101/x-tweet-fetcher>，曾指向 `1498469`）。
现为 **newhigh 主仓库内普通目录**，机器人/本机对 `scripts/` 等的改动由主仓库统一版本管理。

**最近同步**：自上游 `main` 分支 zip（`codeload.github.com/.../x-tweet-fetcher/zip/refs/heads/main`），与目录内 `VERSION` 一致；下次可同样方式或 `git clone` 对比后再 `rsync`。
